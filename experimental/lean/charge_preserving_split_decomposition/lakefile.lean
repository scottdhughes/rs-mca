import Lake
open Lake DSL

package «charge_preserving_split_decomposition» where

@[default_target]
lean_lib «ChargePreservingSplitDecomposition» where
  roots := #[`ChargePreservingSplitDecomposition]
