# Contract: rank-twelve basis-graph ray payment

## Exact parent

This packet is one scoped successor to

```text
ed556ccb7527e1c54e58b8d151ccefd8539000ac
```

with parent canonical payload

```text
edef5ffa88a495a0a659a62a3ce891372b59458350ef4eab5b35f75ed5f37baa
```

The parent proves the source-bound common-locator floor for every proper
rank-two drop. This packet consumes that interface without altering its
first-match or frozen-support semantics.

## Result boundary

The packet proves:

1. every proper rank-two drop with ambient dimension
   `778,970 <= K <= 1,048,576` is impossible;
2. the full rank-two family of `5,170,912` slopes therefore shortens intact
   through all `269,607` dimensions in that interval;
3. for `774,075 <= K <= 778,969`, the synchronized single-ray branch is
   already paid, so any surviving proper drop emits a nonzero near-MDS
   basis-edge word;
4. at `K=778,969`, that word has residual factor degree at most three.

The packet does **not** pay affine error rank twelve, move an active-v4
ledger atom, or close KoalaBear.

## Ownership and chronology

Every count is in distinct original finite slope units. The partition by
whether the frozen exact support meets the actual common core is disjoint.
No local locator charge, basis edge, ray, pair type, or support is summed
through multiple owners.
