## Summary

Stacked on exact parent
`ed556ccb7527e1c54e58b8d151ccefd8539000ac`.

The parent proves a source-bound common-locator floor for every proper
rank-two drop. This PR replaces the archived six-support synchronization
argument by a sharper quotient-matroid theorem.

For the outside-core family, take vector second divided differences of the
frozen minimizing-pair explanations and project modulo the one-dimensional
proper-drop direction. Nonzero projected triples are bases of a rank-three
vector matroid. A nonbasis triple has zero vector second difference whenever
`3r<R+1`. For adjacent bases, the normalized second differences differ in
the leaf direction and are supported in only **four** omission sets. Hence
`4r<R+1` makes adjacent normalized bases equal. Connectivity of the matroid
basis graph synchronizes every basis, placing the complete outside-core
family in one affine correction ray.

The exact threshold is

```text
r <= 262,144
common-locator degree >= 718,960
first ambient K = 778,970
```

A new universal-core-aware affine-line count pays that ray. The exact global
accounting is

```text
rank-one family       4,070,947
single affine ray     1,067,271
--------------------------------
proper-drop cap       5,138,218
rank-two load         5,170,912
slack                    32,694
```

Therefore every proper drop is impossible for
`778,970<=K<=1,048,576`; the whole `5,170,912`-slope family shortens intact
through all `269,607` dimensions and reaches `K<=778,969`.

## Sharpened terminal below the paid interval

For `774,075<=K<=778,969`, the synchronized-ray branch remains below the
rank-two load. Thus any surviving proper drop emits a nonzero near-MDS
basis-edge word. The maximum residual factor degree in this conditional
window is `22,063`.

At the first unresolved cell `K=778,969`, the synchronized branch still has
slack `32,688`; any survivor therefore has a basis-edge word of weight between
`R+1` and `R+4`. After cancelling the actual common core and all outside
zeros, its residual factor has degree at most three. The exact terminal is
`CUBIC_NEAR_MDS_BASIS_EDGE`.

At `K=774,074`, the synchronized-ray arithmetic exceeds the load by exactly
one, giving the exact lower wall of this conditional compiler.

## Scope

- rank-twelve proper drops for `K>=778,970`: paid;
- affine error rank twelve: not yet paid;
- active-v4 ledger movement: `0`;
- KoalaBear closure: not claimed.

All counts are in disjoint original slope units. The proof does not sum local
locator certificates or basis edges.

## Verification

- exact scan of all `269,607` paid ambient cells;
- exact scan of all `4,895` conditional near-MDS cells;
- exact four-endpoint ray optimization;
- independent arithmetic and finite-geometry implementation: PASS;
- finite quotient-matroid configurations: `128`;
- basis vertices/edges checked: `1,002 / 2,400`;
- affine arrangement controls: `10,444` total;
- hostile mutations: `8/8` rejected;
- Python bytecode and standalone TeX fragment compilation: PASS;
- Wolfram selected-cell and convexity replay: PASS;
- no external theorem is load-bearing.

Canonical payload: `e8f0406d912e9a00059db798ec4a6f58c4ccdc6bfa24ce8432fc2ef19e82ea46`.

## Next theorem

Classify the cubic near-MDS basis-edge terminal at `K=778,969`. Its four
omission sets cover the support of a one-dimensional direction word with
only three units of overlap/slack. The strongest route is an exact
near-partition theorem that either synchronizes the basis labels after all or
turns the cubic quotient into one canonical fixed-owner/locator atom.
