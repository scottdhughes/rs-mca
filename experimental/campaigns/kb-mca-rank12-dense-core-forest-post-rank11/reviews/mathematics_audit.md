# Adversarial mathematics audit

Verdict: **GREEN for the stated route cut; RED for any rank-twelve payment or
KoalaBear closure claim.**

## Load-bearing checks

1. Distinct pair cores intersect in at most `K-1`; otherwise both endpoint
   differences have `K` roots and the pair types coincide.
2. The Plotkin prefix is derived from the exact incidence identity and is used
   only when its denominator is positive.
3. Ordinary pair prefixes are used only under `Q^2 < p^6`.
4. The monotone closure enlarges raw caps and cannot create a false upper
   bound.
5. The greedy resource fill is integral-optimal because both slot costs and
   the high-record cost are ordered by deficiency.
6. The forest recurrence uses `max`, not `min`, between full and proper
   outcomes.
7. At `K=s`, a full vanishing direction code is impossible because it would
   contain the constant word.
8. The dynamic program is evaluated at every integer dimension through
   `1,048,576`; no monotonicity assumption replaces the scan.
9. The target and adjacent target are both replayed. The adjacent excess is
   treated as a method wall only.
10. The route terminal is direction rank at most two at an unspecified
    shortened dimension. The full-code star calibration is explicitly
    conditional.

## Mandatory negative control

The local endpoint inequalities admit a fifteen-star abstract set system.
Thus fixed-pair exception injection and bounded core intersections cannot be
summed independently to close the branch.

## First open implication

The missing theorem is aggregate and source-bound: synchronize or count the
leaves of the nested shortening/rank-drop forest. Existing design-matrix,
subspace-design, and intersection theorems do not directly supply this
because the leaf spaces and coordinate locators are selected adaptively from
one received line.
