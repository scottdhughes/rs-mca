---
workboard_item: K4
row: KoalaBear MCA
object: MCA
target_epsilon: 2^-128
agreement: 1116048
B_star: 274980728111395087
unit: distinct bad affine slopes per received line
parent_commit: d01c546f4dca70e256c18c142873821b3bb48ab5
status: PROVED_RANK12_DENSE_CORE_FOREST_ROUTE_CUT
active_v4_ledger_movement: 0
---

# Contract

This packet is a one-commit successor to the affine-error-rank-eleven
global-core payment at exact parent
`d01c546f4dca70e256c18c142873821b3bb48ab5`.

It attacks affine error rank twelve. After the intrinsic near-rational
charge and reversible rank gauge, the explanation direction dimension is at
most eleven. The packet combines three source-level operations:

1. complete minimizing-pair core incidence;
2. a coupled pair-type/resource ceiling using ordinary affine pair lists,
   bounded core intersections, and a Plotkin--Cauchy prefix;
3. an exact dynamic program which retains both possible outcomes at every
   heavy coordinate: whole-family shortening or a proper-span rank drop.

The result is a sharp structural route cut:

```text
every over-budget rank-12 line
  -> one source-bound descendant with >= 9,342,183 slopes
     and explanation direction dimension <= 2.
```

For the declared nine sparse-cap cells, `9,342,184` is the exact adjacent
failure: its evasion threshold exceeds the initial post-near load by
`20,216,453,405`.

## Scope

- rank-twelve route to a rank-at-most-two forest: proved;
- selected sparse cap cells and exact dynamic program: proved;
- active-v4 ledger movement: `0`;
- affine error rank twelve paid: no;
- KoalaBear closed: no.

The output is one chronology-preserving descendant, not a sum of leaf
charges. The remaining object can be a genuine multi-star forest. No
independent per-leaf, per-pair, or per-locator payment is authorized.
