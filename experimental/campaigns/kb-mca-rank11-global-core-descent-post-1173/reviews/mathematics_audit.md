# Adversarial mathematics audit

Verdict: **GREEN for the stated affine-error-rank-eleven branch payment; RED
for KoalaBear closure or active-v4 ledger movement.**

## Load and resource

The initial post-near load is `B*-134944+1`, not `B*+1`.  The complete pair
core lower bound follows on the frozen exact support from the same minimizing
direction used in `theta_gamma`.  Summing core sizes therefore legitimately
uses the pointwise `sum theta` resource.

## Full-span implication

Every incident pair difference vanishes at the heavy coordinate.  If these
differences span the full current direction code, evaluation at that
coordinate annihilates the entire code.  Since every selected minimizing pair
differs from the incident base pair by words in that code, every selected pair
is incident.  This is a whole-family conclusion; no local-core owner is being
summed.

## Shortening semantics

The proof uses each explanation's complete scalar agreement domain.  It does
not replace coordinates in an arbitrary exact support.  A shortened pair
explanation lifts by multiplication with the locator and addition of the
interpolating codeword pair, so same-domain pair noncontainment is preserved.
The same lift preserves the post-near distance condition.  If every exact
new-threshold subset of the shortened complete domain were pair-contained,
RS interpolation uniqueness on adjacent subsets would give one pair on the
whole domain; therefore an exact noncontained support exists.  After
shortening, minimizing pairs may be refrozen.

## Descent

A proper incident pair-difference span places the gauged explanations in a
strictly lower-dimensional RS direction space.  If the actual drop is more
than one, skipping ranks only strengthens the printed load table.  At `K=s`,
full span cannot vanish at one coordinate because the direction space is the
complete degree-`<s` code and contains `1`.

The load-bearing monotonicity is finite, exact, and exhaustive.  The verifier
checks every integer dimension in every rank row and finds no decrease.  The
separate audit reconstructs this scan independently.

## Rank-one endpoint

The low-dominant cross-pair charge is injective because two distinct affine
graph lines meet in at most one finite point.  In the high-dominant branch,
each other dominant line can contribute to at most one point on a fixed
charged line.  All remaining points consume disjoint non-dominant line
weights.  The effective deficiencies only relax this resource, never
underestimate it.

For fixed other variables, the high objective is either convex or strictly
increasing in one deficiency, proving endpoint reduction.  The remaining
finite enumeration is exact.  Low and high points form a disjoint partition.

## Nonclaims

The theorem does not pay rank twelve, identify a v4 owner, or prove that all
other KoalaBear branches fit the row budget.
