# Wolfram exact replay

Wolfram Language independently:

1. reconstructed the three rational Johnson kernels from falling factorials;
2. solved the equality constraints at intersections `3104`, `3105`, and `4356` over the rationals;
3. checked all `4357` dual inequalities exactly;
4. found minimum dual value `1` exactly at those three intersections; and
5. returned integer floor `54,568,751` for the dual objective.

No machine-real result is used by the proof or certificate.
