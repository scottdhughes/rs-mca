# Independent mathematics audit

Status: GREEN for the stated structural router; RED for any row-payment claim.

## Audited implications

1. The predecessor slack gives exactly `L0=2,007,222,636,725` disjoint
   nontransverse slopes on an unsafe line.
2. Every nontransverse row-space packet admits an exact-dimensional parent
   between the source row space and the predecessor witness space.
3. Merging equal parents preserves both disjoint slope ownership and at least
   `42,453` common zeros, because the fixed parent vanishes on the union of
   all source witness sets assigned to it.
4. The dimension-two and dimension-three packet caps are valid applications of
   the ordinary affine projection theorem, the sextic sub-square interleaving
   collapse, and the fixed-pair multiplier.
5. The `508` and `478` abundance bounds follow from literal capacity
   inequalities, not an average packet-size assumption.
6. The pair/triple/fourfold intersection floors use only the identity
   `sum intersection sizes=sum binomial(degrees,k)` and convex balancing.
7. Weighted pinning is chronology-safe because parent packets are disjoint and
   each uses a canonical common-zero subset of the same anchor-good universe.
8. The one-coordinate pinned union lies in one affine pair space with direction
   equal to the span of its parents. If that span had dimension at most four,
   the exact dimension-four pair-list cap would contradict the pinned load.

## Boundary checks

- `477(R2+R3)` falls short of the residual by `3,425,283,800`.
- The synchronized pair floor is `1,530`; replacing it by `1,529` is rejected.
- The one-pin load exceeds the dimension-four cap by `13,469,327,188`.
- The two-pin load exceeds the dimension-two cap by `2,656,640,214`.

## Nonclaims retained

The packet does not show that the dimension-five pinned subfamily exceeds its
own unrestricted cap, does not assign it to an active-v4 first-match owner,
and does not permit independent cancellation of the synchronized locators.