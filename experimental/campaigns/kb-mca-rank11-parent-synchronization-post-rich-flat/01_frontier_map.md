# Frontier map

## Exact parent

Anchored rich-flat head
`2788d5ec3fb4b1d6f9c43a58a86ec2381e5f6804`.

## Imported terminal

At `tau=1547`, every over-budget post-near rank-eleven line has at least
`2,007,222,636,725` slopes in nontransverse low-margin row-space packets.
Each packet emits a strictly larger direction subspace with at least `42,453`
common actual anchor-good zeros.

The imported statement guarantees existence of one flag. It does not quantify
how many rich parents are forced or synchronize their locators.

## New route

Canonicalize each nontransverse packet to an exact-dimensional parent:

- rank-one row spaces map to dimension-two parents;
- rank-two row spaces map to dimension-three parents.

The packet cap is `247,628,052` in dimension two and `3,953,204,973` in
dimension three. Since the packets are disjoint, the imported residual load
forces at least `508` distinct parents total, and at least `478` parents of one
fixed dimension.

For `478` sets of size `42,453` in an anchor universe of size at most
`1,116,048`, exact convexity of binomial degree moments gives:

```text
some pair intersection       >= 1,530
some triple intersection     >=    53
some fourfold intersection   >=     2
```

Two distinct dimension-two parents therefore span dimension at least three;
two distinct dimension-three parents span dimension at least four. Their sum
retains the common locator supplied by the intersection.

A weighted incidence count is stronger in another direction. Some actual
coordinate belongs to parent packets carrying at least
`76,352,112,631` slopes. If their span had dimension at most four, the exact
sub-square interleaved-list cap would be only `62,882,785,443`. Thus the span
has dimension at least five. Similarly, two common coordinates support at
least `2,904,268,266` assigned slopes and force span dimension at least three.

## New successor terminal

Every unsafe line now supplies both:

1. a synchronized same-dimension pair whose sum has dimension at least
   `3` or `4` and common-factor degree at least `1,530`; and
2. a one-coordinate factor supporting a chronology-disjoint subfamily of at
   least `76,352,112,631` slopes whose direction span has dimension at least
   `5`.

## Strongest next theorem

Use the pinned dimension-five subfamily to prove a source-bound shortening
owner, or combine the abundant same-dimensional parents through a
Wronskian/Pluecker rank argument. Per-parent cancellation remains forbidden.