% Integration fragment: rank-eleven centered support-flag router.
% Exact parent: 193b7bf99a5cc7ccea042f25677e698d9f988eee.
% This fragment is intentionally standalone source material and moves no
% active-v4 ledger atom.

\begin{theorem}[Centered two-level support-flag router]
\label{thm:kb-rank11-factor-flag-router}
Retain the actual post-near KoalaBear affine-error-rank-eleven record selection
and one frozen minimizing pair for every selected slope.  Let $C'$ be the
gauged explanation-direction code, so $\dim C'\le10$.

Then every line with more than
\[
 B_*=274980728111395087
\]
bad slopes contains an actual low-margin minimizing pair $e$, a dense center
pair $e_*$, and a three-dimensional subcode $W\le C'$ such that
\[
 \operatorname{span}\{a_e-a_{e_*},b_e-b_{e_*}\}\le W
\]
and every word of $W$ vanishes on at least $23354$ common deployed
coordinates contained in a fixed $1116044$-subset of the complete center
core.
\end{theorem}

\begin{proof}
Use the parent weighted-pair theorem at cutoff $1795$ to choose $e_*$ with
complete-core deficiency at most four.  At cutoff $\tau=1936$, every low pair
core has size at least $1114112$, so its pair-difference row space from $e_*$
has at least $133004$ common zeros in the fixed center subset.

Assume no relevant three-dimensional extension has $23354$ common zeros.
Assign each rank-one row space either to a two-plane with at least $117731$
zeros or to a terminal line, and assign each rank-two row space to itself.
Ordered bases of evaluation-normal annihilator flats give at most
\[
 8415196932
\]
terminal lines and
\[
 382360905
\]
two-plane containers.  The interleaved affine-list caps are respectively
$15$ and $255$, and each pair type owns at most $983040$ slopes.  Including
the center pair once gives the low-margin bound
\[
 219935524214538240.
\]
The nonuniform support-margin resource bounds the high tail by
$55043143075392992$; adding the disjoint near charge $134944$ gives
\[
 274978667290066176<B_*.
\]
The contrapositive proves the theorem.
\end{proof}

\begin{remark}
At the same cutoff, raising $23354$ to $23355$ gives the exact optimized total
$274995846032030976>B_*$.  The theorem moves no active-v4 ledger mass and
does not pay affine error rank eleven.
\end{remark}
