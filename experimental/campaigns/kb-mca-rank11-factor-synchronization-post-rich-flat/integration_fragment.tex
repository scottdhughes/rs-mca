% Candidate integration fragment.  Source maintainers should place this only
% after the anchored rich-flat theorem and preserve the exact parent guards.

\begin{theorem}[Weighted parent-factor synchronization]
Assume the deployed KoalaBear row and the selected post-near
affine-error-rank-eleven interface of the anchored rich-flat router.  If the
received line has more than \(B_*\) bad slopes, then at cutoff
\(\tau=1549\):

\begin{enumerate}
\item at least \(27749\) distinct dimension-two or dimension-three parent
direction spaces occur, each vanishing on at least \(42448\) coordinates of
one actual anchor-good support;
\item some two parents have at least \(1614\) common zero coordinates, some
three have at least \(62\), and some four have at least \(3\);
\item for every \(1\le k\le10\), one \(k\)-coordinate set is contained in
parents carrying at least the load \(\Lambda_k\) listed in the certificate.
\end{enumerate}

The sum spaces in the second clause have dimensions at most \(6,9,10\),
respectively.
\end{theorem}
