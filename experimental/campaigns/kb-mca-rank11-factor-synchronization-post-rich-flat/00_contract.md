---
workboard_item: K4
row: KoalaBear MCA
object: MCA
target_epsilon: 2^-128
agreement: 1116048
B_star: 274980728111395087
unit: distinct bad affine slopes per received line
parent_commit: 2788d5ec3fb4b1d6f9c43a58a86ec2381e5f6804
status: PROVED_STRUCTURAL_MASS_AND_SYNCHRONIZATION_ROUTER
active_v4_ledger_movement: 0
---

# Contract

This packet is a one-commit successor to the anchored rich-flat router at
exact parent `2788d5ec3fb4b1d6f9c43a58a86ec2381e5f6804`.

The parent pays the full `h=42447` transverse row-space envelope at cutoff
`tau=1549` by

```text
274,871,033,266,908,609,
```

leaving exact slack

```text
109,694,844,486,478.
```

Consequently an over-budget line carries at least

```text
109,694,844,486,479
```

distinct slopes in nontransverse rank-one/rank-two row-space groups.

Each such group is routed canonically to an immediate parent direction space
of dimension two or three.  Every parent has at least `42,448` common actual
anchor coordinates, and one parent owns at most `3,953,213,019` assigned
slopes.  Therefore an unsafe line has at least `27,749` distinct parent
spaces.

The resulting set system on the actual anchor-good support satisfies the
following exact synchronization cascade:

```text
two parents:    at least 1,614 common coordinates, sum dimension <= 6
three parents:  at least    62 common coordinates, sum dimension <= 9
four parents:   at least     3 common coordinates, sum dimension <= 10
five parents:   at least     1 common coordinate
```

There is also a weighted form.  Some fixed `k`-coordinate set is contained in
parents carrying at least the following assigned slope load:

```text
k=1    4,172,156,357,758
k=2      158,681,059,954
k=3        6,035,034,641
k=4          229,522,148
k=5            8,728,902
k=6              331,960
k=7               12,625
k=8                  481
k=9                   19
k=10                   1
```

## Quantifier

Uniform over the deployed sextic KoalaBear line field, every received pair,
every selected post-near affine-error-rank-eleven family, every explanation
direction space of dimension at most ten, and the actual minimizing-pair and
anchor selection frozen by the predecessor stack.

## Projection and ownership

All loads count distinct finite affine slopes.  The predecessor's row-space
partition is disjoint.  A deterministic immediate-parent rule is fixed once
and merges coincident parents before counting.  Thus every residual slope is
assigned exactly once, and the per-parent pair-list cap is applied only after
that partition.

## Exact impact

- replaces one emitted common-factor flag by an abundance theorem for at least
  `27,749` distinct parent flags;
- forces exact pair/triple/quadruple common-locator synchronization;
- forces weighted slope mass on fixed shared coordinate sets;
- makes zero active-v4 ledger movement;
- does not pay rank eleven or close KoalaBear.
