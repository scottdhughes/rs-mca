# Frontier map

## Exact parent

Anchored rich-flat head `2788d5ec3fb4b1d6f9c43a58a86ec2381e5f6804`.

## Imported terminal

At `tau=1549`, the predecessor transverse envelope is below the KoalaBear
budget.  Every group not charged by that envelope is a rank-one or rank-two
row-space group with a proper annihilator flat containing at least `42,448`
labeled columns from one actual anchor-good support.

The predecessor records only existence of one such flag.

## New theorem

The budget slack converts existence into mass.  In an unsafe line,
nontransverse groups carry at least `109,694,844,486,479` slopes.

Route each nontransverse row space `U` to a canonical immediate parent `W`:

```text
dim U = 1 -> dim W = 2,
dim U = 2 -> dim W = 3.
```

The selected heavy flat ensures that every polynomial in `W` vanishes on at
least `42,448` common actual anchor coordinates.  Merging equal parents is
chronology-safe because the original row-space groups are disjoint.

The dimension-three interleaved pair cap gives at most
`3,953,213,019` assigned slopes per parent.  Hence at least `27,749` parents
occur.

## Exact synchronization

For associated common-zero sets `J_i`, choose one canonical `42,448`-subset
from each.  On a universe of size at most `1,116,048`, convexity of
`binomial(d_x,k)` yields:

| selected parents | forced common coordinates | sum-space dimension |
|---:|---:|---:|
| 2 | 1,614 | at most 6 |
| 3 | 62 | at most 9 |
| 4 | 3 | at most 10 |
| 5 | 1 | at most 10 |

The sum of the selected parent spaces is divisible by the locator of the
displayed common coordinate set.

## Weighted terminal

A fixed `k`-coordinate set supports the exact load profile frozen in the
certificate.  This is stronger than an unweighted incidence statement and is
the preferred input to the next owner/shortening theorem.

## Next joint

Prove that one synchronized sum-space is either:

1. an existing chronology owner after common-locator cancellation; or
2. a low-rank Sylvester/subresultant component whose assigned slope load is
   below the corresponding weighted threshold.

A per-parent or per-edge sum remains forbidden.
