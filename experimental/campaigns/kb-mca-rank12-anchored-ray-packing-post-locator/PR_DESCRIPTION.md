## Summary

Stacked directly on exact parent
`ed556ccb7527e1c54e58b8d151ccefd8539000ac`.

This PR replaces the six-support one-ray wall by an anchored packing theorem.
Fix two exceptional supports.  Distinct normalized second divided differences
have pairwise-disjoint triple-agreement sets; any shared coordinate would make
two independent direction words vanish there and create a forbidden
whole-family pair core.  Consequently the exceptional family is a disjoint
union of at most

\[
\left\lfloor\frac{N-2r}{N-3r}\right\rfloor
\]

affine correction rays.

The parent locator floor makes this number at most four for every actual
proper drop once `K>=706,612`.

Each ray is paid by a universal-core-aware weighted affine-line theorem.  For
unknown nonuniversal threshold `M>=67,473`, parity convexity reduces the full
range to a finite endpoint table.  The exact branch caps are

```text
ray count   certified cap   slack to rank-two load
1             4,460,342                 710,570
2             4,908,361                 262,551
3             4,425,931                 744,981
4             4,937,277                 233,635
```

Therefore a proper rank-two drop is impossible throughout

```text
706,612 <= K <= 1,048,576.
```

All `5,170,912` slopes shorten intact through `341,965` ambient dimensions and
reach `K<=706,611`.

## Exact adjacent wall

At `K=706,611`, the parent floor gives

```text
k = 69,545
c = 637,066
r = 344,038
floor((N-2r)/(N-3r)) = floor(430045/86007) = 5.
```

The direct five-ray envelope is `5,417,198`, above the rank-two load by
`246,286`.  This is a method wall, not an unsafe certificate.

## Scope

- rank-twelve proper-drop branch above `K=706,611`: paid;
- affine error rank twelve: not yet paid;
- active-v4 ledger movement: `0`;
- KoalaBear closure: not claimed.

The next theorem must exploit interaction among five anchored correction rays;
a sum of five independent ray caps is insufficient.

## Verification

- exact parent locator-floor replay through all `341,965` ambient cells;
- exact anchored ray-count intervals for counts one through four;
- exact weighted-ray parity endpoint theorem;
- maximum ray finite difference `4`;
- rank-one minimum finite difference `12`;
- primary verifier: PASS;
- independent arithmetic and finite-model audit: PASS;
- hostile mutations: `8/8` rejected;
- Wolfram exact boundary and convexity replay: PASS;
- targeted current literature review: no external theorem is load-bearing.
