# Certificate and custody review

Status: **GREEN**.

The certificate recomputes the selected envelope from primitive integer
parameters, constructs nested loads recursively, recomputes all ten affine
list and slope caps, and verifies every strict dimension comparison.  The
independent audit uses product recurrences and rational arithmetic rather than
the primary binomial implementation.

The manifest binds the exact parent, all packet files, result payload, claims,
and nonclaims.  Hostile mutations of the cutoff, threshold, residual load,
nested load, dimension floor, field guard, parent SHA, and closure flags are
rejected.
