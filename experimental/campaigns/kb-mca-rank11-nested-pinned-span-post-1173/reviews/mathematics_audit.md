# Adversarial mathematics audit

Verdict: **GREEN for the stated nested structural router; RED for row closure.**

## Envelope

The selected total is the same disjoint near/high/anchor/transverse envelope
proved by PR #1173.  The residual lower load uses `B*+1-total`, not
`B*-total`.

## Nested greedy step

After conditioning on `T_j`, every surviving parent contains exactly `q-j`
additional selected common-zero coordinates.  Weighted double counting is an
identity.  Choosing a maximum-load next coordinate preserves nestedness and
cannot duplicate a slope.

## Sum-space step

All parents containing `T_k` vanish on `T_k`; therefore so does their sum
space.  Every assigned endpoint-difference row lies in that sum.  Coincident
parents were merged before pinning.

## Dimension step

The pair-list cap is applied once to the complete affine product generated by
`V_k`, not separately to its constituent parents.  The fixed-pair multiplier
is then applied per distinct pair type.  All ten sextic field guards hold.

## Scope

The argument produces nested high-dimensional common-zero spaces, not a
chronology owner.  It does not assert that the nested spaces are represented,
independently chargeable, or cancellable without a source adapter.
