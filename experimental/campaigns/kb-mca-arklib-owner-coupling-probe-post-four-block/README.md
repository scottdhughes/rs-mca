# ArkLib owner-coupling research probe

This packet records the useful transfer from the current
`elizaOS/proximityprize` / ArkLib line-list machinery to the exact
rank-twelve rs-mca wall at `K = 662,479`.

It proves only an exact arithmetic consequence of the proposed shared-fiber
coupling: the formerly independent relaxed extremal owner profile drops from
`1,099,983` to `828,956`, a saving of `271,027`.

Replay:

```bash
python3 experimental/scripts/verify_kb_mca_arklib_owner_coupling_probe_v1.py
python3 experimental/scripts/verify_kb_mca_arklib_owner_coupling_probe_v1.py --tamper-selftest
```

The adjacent cell is not declared paid.  The next theorem must optimize the
coupling over every feasible dirty-incidence profile.
