# M1 quadratic-parameter mask audit and post-C5 incidence split v1

**Status:** PROVED NEW POLICY-INCOMPATIBILITY ROUTE CUT / CERTIFIED APPLICATION
OF INTEGRATED C5 AND RANK CLOSURES / PROVED NEW TWO-COLUMN CASE SPLIT / ROW
OPEN.

This packet continues the degree-two parameter-field attack for the KoalaBear
MCA row at

\[
A=1{,}116{,}048.
\]

It does not invent missing first-match equations.  Instead it machine-checks
the frozen source-status inventory for the KoalaBear branch-1--5 predicates,
applies the integrated proper-projective-field C5 theorem as a separate
refinement inside branch 6, and classifies the exact scalarized two-column
incidence.  This is not a repository-wide nonexistence search for alternative
adapters outside the declared source bundle.

Applying the imported results from integrated PRs #660 and #670 closes two
subcases in the new partition:

1. syndrome rank zero has no noncontained witness;
2. after canonical C5 removal, syndrome rank one contributes at most one
   finite slope.

The surviving field-full rank-two support union remains an explicit unpaid
route cut.  No value is assigned to \(U_2\), \(U_Q\), or \(U_A\).

The new contribution of this packet is the KoalaBear/projective policy
mismatch, the frozen mask-status inventory, the exact quotient-pair census and
basis-relative pivot atlas, the full-field quadratic compatibility control,
and the resulting field-full rank-two unpaid route cut.  Canonical C5,
rank-zero emptiness, the rank-one one-slope lemma, and the fixed-low-excess
owner are imported and only applied here.

## Statement audited

Let

\[
B=\mathbb F_p\subset K=\mathbb F_{p^2}\subset F=\mathbb F_{p^6},
\qquad p=2{,}130{,}706{,}433,
\]

and retain the predecessor degree-two slope condition

\[
\gamma\in K\setminus B.
\]

The frozen KoalaBear branch order begins

```text
1. contained_or_noncontained_failure
2. rank_drop_or_pivot_failure
3. tangent_common_line_residue
4. quotient_periodic_or_divisor_stabilized
5. planted_prefix_structured
6. extension_valued_slope
```

The projective-syndrome catalogue instead begins

```text
C1 quotient
C2 Chebyshev / dihedral
C3 planted
C4 tangent / deep / common-line
C5 extension / proper-field descent
```

These are not the same ordered partition.  In particular, KoalaBear branch 5
is planted/prefix structure, while projective C5 is proper-field descent.
Therefore the predecessor residual after KoalaBear branches 1--5 is not, by
definition, post-projective-C5.

## Exact branch-1--5 mask inventory

The executable source status is:

| KB branch | exact machine status | consequence |
|---|---|---|
| 1 containment interface | `MACHINE_EXACT_GATE_ONLY` | On a fixed support, simultaneous containment is exactly the vanishing of all six scalarized quotient syndromes.  The source does not make this interface bucket a paid slope projector. |
| 2 rank/pivot | `UNBOUND_SOURCE_SYMBOL` | The source requires a surviving pivot such as \(\operatorname{red}_p B_0(S)\ne0\), but supplies no deployed support-to-\(B_0\) builder or complete pivot order. |
| 3 tangent/common-line | `SOURCE_STATUS_ONLY` | No row-specific matrix, independent-minor list, or actual slope projector is supplied. |
| 4 quotient/periodic | `PARTIAL_FAMILY_NO_COMPLETE_PROJECTOR` | Q0 supplies exact predicates for declared dyadic rungs, but lower-rung obligations and large planted tails remain open; it is not a complete branch projector. |
| 5 planted/prefix | `SOURCE_STATUS_ONLY` | No exhaustive algebraically planted family or complete prefix-structured slope projector is supplied. |

Thus the frozen source bundle does not supply an exact branch-1--5 mask
adapter.  Its first missing executable predicate is branch 2.  This is a
source-interface route cut, not a failed computation or a claim that no
alternative adapter exists anywhere in the repository.

## Certified application of integrated projective C5

Let

\[
Y_R=[y_0\ y_1]
\]

be the global syndrome matrix of the received pair and let
\(d=\operatorname{rank}_F Y_R\).
For \(e\in\{1,2,3\}\), coefficientwise Frobenius gives

\[
\phi_e(x)=x^{p^e}.
\]

The pair syndrome space is defined over \(\mathbb F_{p^e}\) exactly when

\[
\operatorname{rank}_F[Y_R\mid\phi_e(Y_R)]=d.
\]

Consequently, for \(d>0\),

\[
F_{\rm proj}(R)=F
\quad\Longleftrightarrow\quad
\operatorname{rank}[Y_R\mid\phi_e(Y_R)]>d
\quad(e=1,2,3).
\]

This gives an exact local refinement of KoalaBear branch 6:

```text
6a_projective_proper_field_C5:
  F_proj(R) is F_p, F_(p^2), or F_(p^3);

6b_extension_slope_pair_field_full:
  F_proj(R)=F_(p^6).
```

The canonical C5 theorem, integrated from PR #660, makes 6a
witness-exhaustive for every proper-field pair after whatever realized earlier
cells have been removed.  Therefore every nonempty 6b residual has full
projective syndrome field.

This is coverage, not payment.  The packet does not alias 6a to KoalaBear
branch 5 and does not charge it to an existing finite KoalaBear owner.

The global field test must be applied to \(Y_R\).  Replacing it by a
support-dependent quotient matrix can lose precisely the coordinates that
generate the full field.

## Exact two-column incidence classification

Fix a support \(S\) of size \(A\), put \(E=D\setminus S\), and use the
predecessor scalarization over \(K\).  In

\[
Q_E=
\left(K^{n-k}/V_E(K)\right)^{\oplus3},
\]

the support equation is

\[
\overline{\mathbf y}_0+\gamma\overline{\mathbf y}_1=0.
\]

Let \(m=\dim_K Q_E\) and \(q=|K|\).  Exactly one of the following occurs.

1. \(\overline{\mathbf y}_0=\overline{\mathbf y}_1=0\).
   Both received coordinates are contained on \(S\), so the noncontained
   witness condition fails.
2. \(\overline{\mathbf y}_1=0\) and
   \(\overline{\mathbf y}_0\ne0\).  The affine equation is inconsistent.
3. \(\overline{\mathbf y}_1\ne0\) and the two columns are not proportional.
   The affine equation is inconsistent.
4. \(\overline{\mathbf y}_1\ne0\) and
   \(\overline{\mathbf y}_0=c\overline{\mathbf y}_1\).  There is exactly one
   root,
   \[
   \gamma=-c.
   \]
   A base root is outside the degree-two class; a nonbase \(K\)-root proceeds
   to 6a or 6b according to the global projective field.

The exact ordered-pair census in a quotient space of size \(q^m\) is

\[
\begin{aligned}
N_{\rm contained}&=1,\\
N_{\rm zero\ second\ inconsistent}&=q^m-1,\\
N_{\rm unique\ root}&=q(q^m-1),\\
N_{\rm nonproportional\ inconsistent}
 &=q^{2m}-(q+1)q^m+q.
\end{aligned}
\]

These four numbers sum to \(q^{2m}\).

For the deployed scalarized quotient vector length

\[
M=3(A-k)=202{,}416,
\]

fix any ordered \(K\)-basis of \(Q_E\) for each support \(E\).  Relative to
that declared basis, the unique-root locus has a disjoint pivot atlas.  If
\(j\) is the first nonzero coordinate of
\(v_E=\overline{\mathbf y}_1\), impose

\[
\begin{aligned}
v_{E,i}&=0 &&(i<j),\\
v_{E,j}&\ne0,\\
v_{E,j}u_{E,\ell}-u_{E,j}v_{E,\ell}&=0 &&(\ell\ne j),\\
\gamma&=-u_{E,j}/v_{E,j},\\
\gamma^p-\gamma&\ne0.
\end{aligned}
\]

There are exactly \(202{,}416\) possible pivot labels per support and per fixed
ordered basis, and the first-nonzero rule makes them disjoint and exhaustive
on the nonbase unique-root locus.  No deterministic support-to-quotient-basis
adapter is supplied.  This is an exact basis-relative compressed incidence
atlas; it is not the missing KoalaBear branch-1--5 mask atlas.

## Global syndrome-rank split

Applying the integrated rank lemmas from PRs #660 and #670 before support
unioning:

- `rank(Y_R)=0`: both received words are codewords, so the noncontained witness
  incidence is empty;
- `rank(Y_R)=1`: writing \(y_0=\alpha z\), \(y_1=\beta z\), every finite bad
  slope satisfies \(\alpha+\gamma\beta=0\), so there is at most one;
- `rank(Y_R)=2`: each fixed support is empty or contributes one root, but
  different supports may contribute different roots.

Hence the field-full rank-one subcase has a uniform exact-root union of degree
at most one.  This component is closed.  Across received-pair rank cases the
uniform operation is a maximum, not a sum, so the one-slope result is not added
as a separate global charge while the rank-two case remains open.

There is also an imported existing owner from PR #670 for any rank-two
residual family whose discrepancy supports are all contained in one fixed set
\(U\) of size \((n-k)+2\).  The agreement-weighted transverse-secant theorem
gives

\[
\left\lfloor
\frac{\binom{(n-k)+2}{3}}
     {\binom{A-k+1}{2}}
\right\rfloor
=84{,}416{,}263.
\]

This fits the current remainder, but no witness-exhaustive subexponential
cover by such low-excess sets \(U\) is known.  It is therefore a named paid
owner for compatible components, not a payment of the whole rank-two class.

## Full-field rank-two compatibility control

The Sage replay constructs an actual weighted RS syndrome example over

\[
\mathbb F_7\subset\mathbb F_{7^2}\subset\mathbb F_{7^6}
\]

with \(D=\{0,1,2,3\}\), \(n=4\), \(k=1\), and a two-point discrepancy support.
Let \(h_0,h_1,h_2\) be three independent weighted Vandermonde parity columns,
choose \(\alpha\) of degree six over \(\mathbb F_7\), and choose
\(\gamma\in\mathbb F_{7^2}\setminus\mathbb F_7\).  Put

\[
u=h_0+\alpha h_1,\qquad
y_1=h_2,\qquad
y_0=u-\gamma y_1.
\]

Then

\[
y_0+\gamma y_1=u\in\operatorname{span}_F\{h_0,h_1\},
\]

while \(y_1\) is outside that span, so the support is noncontained.  The
two-dimensional syndrome space

\[
\operatorname{span}_F\{h_2,h_0+\alpha h_1\}
\]

is not stable under the \(p\)-, \(p^2\)-, or \(p^3\)-Frobenius and therefore has
projective field \(F=\mathbb F_{7^6}\).

Thus full projective syndrome field is compatible with a genuine
\(K\setminus B\) fixed-support root.  The control is not claimed to survive the
deployed KoalaBear branches 1--5; it only blocks the invalid implication
`field-full => no quadratic-parameter incidence`.

## Dependencies

- **PROVEN:** the predecessor \(F/K\) scalarization and fixed-support quotient
  equation.
- **PROVEN:** the frozen KoalaBear branch order and each source status printed
  in the mask inventory.
- **IMPORTED / APPLIED (#660):** canonical proper-field C5 coverage,
  rank-zero emptiness, and the post-C5 full-projective-field consequence.
- **IMPORTED / APPLIED (#670):** the rank-one one-slope cap and the fixed
  low-excess \(U\)-chart cap \(84{,}416{,}263\); an exhaustive low-excess
  shadow cover is unproved.
- **PROVEN NEW HERE:** the policy mismatch, frozen source-status inventory,
  two-column case split, exact pair census, and basis-relative
  first-nonzero-coordinate pivot atlas.
- **PROVEN NEW CONTROL:** the exact Sage full-field rank-two compatibility
  example.
- **UNPROVEN:** deployed executable predicates for KoalaBear branches 2--5.
- **UNPROVEN:** a finite KoalaBear payment for the canonical proper-field C5
  refinement.
- **UNPROVEN:** a deduplicated support-to-slope union bound for field-full
  syndrome-rank-two pairs.
- **OPEN:** \(U_Q\), \(U_A\), and the final KoalaBear inequality.

## Parameter dependence

The policy incompatibility is exact for the current repository statements.
The projective-field rank test is uniform in finite Galois field towers.  The
listed proper fields and degree-two parameter class use the specific tower
\(\mathbb F_p\subset\mathbb F_{p^2}\subset\mathbb F_{p^6}\).  The row budget
and open ledger terms are specific to KoalaBear \(A=1{,}116{,}048\).

## Layer-cake / dyadic summability

Not applicable.

## Moment / Markov / Chebyshev

Not applicable.

## Edge cases and notation

- “After KoalaBear branch 5” and “post-projective-C5” are different predicates.
- The proper intermediate fields of \(F/\mathbb F_p\) have degrees \(1,2,3\);
  omitting degree three makes the field-full test false.
- Pair projective field is global; support quotient field is local.
- Projective C5 coverage does not itself provide a finite slope charge.
- Fixed-support uniqueness does not imply \(d_{\rm eff}\le1\).
- Root unions are deduplicated within one received line; across received lines
  the operation is a supremum or maximum.
- `null` is not zero.

## Numerical evidence

The row arithmetic inherited from the predecessor is exact.  The quotient-pair
census is an identity.  The \(\mathbb F_{7^6}\) example is an exact Sage
control, not random evidence and not a deployed first-match survivor.

## Verdict

**GREEN for the new policy-incompatibility route cut, new two-column
classification, new basis-relative pivot atlas, and new compatibility control;
GREEN as certified applications of imported #660/#670 for the C5, rank-zero,
rank-one, and fixed-low-excess subroutes.  RED for a complete deployed mask
replay or a rank-two payment.**

## Remaining risks

The first missing executable mask is branch 2, so algebra on the final
two-column equation cannot certify that a root survives the deployed earlier
owners.  Even after the canonical C5 refinement, proper-field coverage needs a
finite payment and field-full rank-two roots can vary with the support.  A
future low-degree eliminant, low-excess shadow cover, or named owner may still
close that component.

## Minimal next action

Freeze one row-specific branch-2 pivot matrix and its support-to-row adapter.
If that predicate is supplied, replay the same inventory in order and stop at
the next missing projector.  In parallel, the first algebraic residual is now
precisely:

```text
F_proj(R)=F_(p^6),
rank(Y_R)=2,
gamma in F_(p^2) minus F_p,
ybar_0 + gamma*ybar_1 = 0,
survives the eventually completed KB branch-1--5 masks.
```

Do not move to degree three, set \(U_2\), or claim a ledger improvement.

## Replay

```bash
python3 experimental/scripts/verify_m1_fp2_post_c5_mask_incidence_v1.py --check
python3 experimental/scripts/verify_m1_fp2_post_c5_mask_incidence_v1.py --tamper-selftest
/usr/local/bin/sage experimental/scripts/verify_m1_fp2_post_c5_mask_incidence_v1.sage
python3 experimental/scripts/verify_m1_fp2_residual_route_cut_v1.py --check
```
