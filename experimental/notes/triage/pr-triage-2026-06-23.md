# PR Triage: 2026-06-23

Status: AUDIT / NO RAW INTEGRATION.

Scope: open PRs after the previous triage. PRs #97 and #98 were unchanged and
had already been integrated as experimental notes/scripts. PR #96 moved from
`312eedf` to `2965689`.

## Open PRs

| PR | Author | New status |
| --- | --- | --- |
| #96 | DannyExperiments | Updated with Cycle115--120 material. Not mergeable as-is. Compact Cycle119 admissibility review added. |
| #97 | avdeevvadim | Unchanged since prior integration. |
| #98 | scottdhughes | Unchanged since prior integration. |

## PR #96 Delta

New commits inspected:

```text
f3c7613 Cycle84 finite computation note
2f6a4fe Stage Cycle115 Cycle84 transfer packet
2e1d8a4 Audit Cycle115 Cycle84 transfer returns
c11f6ae Stage Cycle116 standalone transfer packet
f04c57a Audit Cycle116 standalone transfer returns
159d62f Add Cycle116 PRZ review note
8afa6b7 Stage Cycle117 source contract packet
a46e9d2 Audit Cycle117 source contract returns
c3c4efa Audit Cycle118 strict-ball returns
02e89ce Stage Cycle119 Role05 proof packet
0f4419c Audit Cycle119 strict-263 returns
0bb1df6 Audit final Cycle119 hostile returns
8511e98 Audit Cycle120 official source gates
2965689 Verify Cycle120 ABF admissibility gates
```

Delta size:

```text
678 files changed
201688 insertions
8 deletions
```

The branch includes raw returns, generated checkers, zips, prompt packets,
extracted PDF copies, rendered pages, and new generated proof-record folders.
These are not suitable for the streamlined `experimental/` policy.

## Useful Content

The useful mathematical claim is Cycle119:

```text
K = F_17^32
H = <theta>, |H|=512
C = RS[K,H,256]
LD_sw(C,263) >= 52,747,567,092
```

If valid, it improves Cycle116's agreement-262 transfer to agreement 263 and
therefore satisfies the strict inequality:

```text
512 - 263 = 249 < 250 = (125/256) * 512.
```

Arithmetic check:

```text
floor(17^32 / 2^128) = 6
52,747,567,092 > 6
```

So the proposed numerator clears the `2^-128` denominator threshold.
Numerically the density is about `2^-95.18`.

## Admissibility Check

Local sources and the public prize page support the following reading:

- standard RS row over a finite field is allowed;
- smooth domain means a multiplicative subgroup/coset of power-of-two order;
- rate `1/2` is in the challenge set;
- `epsilon_mca` uses support-wise same-support noncontainment;
- `gamma` is sampled from the code field in the grand MCA definition.

The PR-supplied ABF extraction and Cycle120 direct ABF audit state these points
directly. The exact ABF PDF should still be independently fetched and checked
before public use, because this triage did not independently retrieve the PDF.

Recommended wording:

```text
Under the printed ABF grand MCA formulation, Cycle119 is a prize-facing
negative counterexample candidate for the row RS[F_17^32,H,256], assuming the
finite computation and two-ended locator transfer are correct. It is not an
accepted prize solution.
```

## Integration

Integrated only:

```text
experimental/notes/m1/m1_cycle119_strict263_admissibility_review.md
```

Skipped:

```text
new generated proof-record folders
cycle115--cycle120 raw/generated folders
zips
prompt packets
generated checkers
copied PDFs/rendered pages
```

## Follow-Up

Ask Danny for a standalone proof note for the two-ended locator transfer and a
separate minimal record of the finite computation it depends on. No generated
archive should be needed for review.

## Follow-Up Update: PR #96 Head `98f1bb4`

The new commit packages a Cycle120 ABF-facing counterexample note plus a zip,
rendered PDF, rendering script, copied ABF source material, and generated
review folders. The branch is still not mergeable as-is.

Useful content integrated in cleaned form:

```text
experimental/notes/m1/m1_cycle120_abf_counterexample_candidate.md
```

Decision:

```text
Do not import the zip, rendered PDF, render script, copied ABF PDF, generated
checkers, large tracker rewrite, or prompt/return archives.
```

Audit result:

```text
Cycle119's two-ended algebra checks as a conditional strict-ball theorem.
The ABF-critical negative result at delta=125/256 actually needs only Cycle116
agreement 262, because ABF uses |S| >= (1-delta)n.
```

Remaining dependencies:

```text
1. independent official retrieval of the ABF PDF;
2. human review of the Cycle84 finite count;
3. human review of the Cycle116 fixed-jet transfer;
4. a nonmutating minimal verifier/reviewer packet.
```
