# PR Triage: 2026-06-22

Status: AUDIT / EXPERIMENTAL.

Scope: open PRs #96, #97, and #98. This pass inspected PR refs with `git`
without running PR code locally. Papers A-D remain unchanged.

## Summary

| PR | Verdict | Integration |
| --- | --- | --- |
| #96 DannyExperiments, Cycle58-114 / Cycle84 replay | Useful but not mergeable as-is | Integrated only a compact Cycle84 public replay audit note. Skipped workflow, raw archives, and generated cycle history. |
| #97 avdeevvadim, F1 deep-point list-to-CA/MCA | Useful | Integrated the note and sanity-check script as experimental F1 material. |
| #98 scottdhughes, L1 Fourier orbit cancellation | Useful | Integrated the note and verifier as experimental L1 material. |

## PR #96: Cycle84 Replay and Large Cycle Archive

Changed surface:

```text
1530 files changed
1177768 insertions
1 deletion
```

The PR contains a live workflow:

```text
.github/workflows/cycle84-certificate-replay.yml
```

and a large raw archive under `experimental/notes/m1/`, including prompt/return
packets, zip bundles, C++/Python replay code, and generated JSON/log material.

The useful part is the finite Cycle84 certificate replay. Public GitHub Actions
metadata for run `27889140962` reports:

```text
Light certificate chain: success
Full projected census and kernel lift: success
head commit: 3914f4d08b6ca5b919c84fe2598e4e74685caec4
```

Banked finite result:

```text
m_max(beta)             = 2
Occ(beta)               = 52,747,567,092
D                       = 24
double fibers           = 12
fibers of size >= 3     = 0
```

Decision:

```text
Integrate compact audit note only:
experimental/notes/m1/m1_cycle84_public_replay_audit.md
```

Do not integrate:

```text
.github/workflows/cycle84-certificate-replay.yml
experimental/notes/m1/cycle84_wallbreaker_returns_raw/
experimental/notes/m1/cycle84_github_replay_workflow/
cycle64--cycle114 raw prompt/response archives
zip bundles and generated artifact dumps
```

Reason: the finite result is interesting and important as a public replayed
finite-model certificate, but the PR packaging adds executable infrastructure
and over a million lines of raw generated provenance. That conflicts with the
current streamlined `experimental/` policy.

Status to use:

```text
AUDIT / FINITE_MODEL_PROOF / PUBLIC_REPLAY
```

Non-claim:

```text
No prize-level theorem.
No corrected MCA theorem.
No main-paper promotion without a transfer theorem.
```

## PR #97: F1 Deep-Point List-to-CA/MCA Conversion

Integrated:

```text
experimental/notes/f1/f1_deep_point_list_to_ca_mca.md
experimental/scripts/f1_deep_point_list_to_ca_mca_sanity.py
```

The script was inspected as text. It is standard-library only, does not import
network/process modules, and no filesystem writes/deletes were visible. It was
not run during this pass.

Why useful:

- Gives a self-contained simple-pole identity converting a nearby
  `RS[F,D,k+1]` list through a deep point `alpha notin D` into CA and
  support-wise MCA bad slopes for `RS[F,D,k]`.
- Gives a direct route for special quotient-locator cap mechanisms, independent
  of the CS25 conversion, if the predicate and constant audit passes.
- Gives an explicit extension-valued line when `D subset B subsetneq F` and
  `alpha in F \ B`.

Conservative status:

```text
PROVED for the simple-pole identity.
CONDITIONAL / AUDIT for the quotient-locator cap application.
```

Promotion warning: do not use this note to undo Paper D's CS25 radius patch
until the CA/MCA definitions, quotient-locator list input, and finite constants
are checked against the main-paper conventions.

## PR #98: L1 Fourier Orbit-Cancellation Reduction

Integrated:

```text
experimental/notes/l1/l1_prefix_fourier_orbit_cancellation.md
experimental/scripts/verify_l1_fourier_orbit_cancellation.py
```

The script was inspected as text. It is standard-library only and imports the
existing local helper `verify_l1_fourier_reduction`. It does not use
subprocess/network modules and no filesystem writes/deletes were visible. It was
not run during this pass.

Why useful:

- Proves exact dual-dilation orbit invariance for the monomial-prefix Fourier
  lane.
- Regroups the prefix Fourier expansion into orbit kernels.
- Records kernel orthogonality and the orbit second-moment identity.
- Gives an important route cut: pointwise orbit-kernel saving can fail even in
  quotient-free prefix slices, so future L1 work needs cancellation, higher
  moments, or orbit-correlation estimates.

Conservative status:

```text
PROVED for orbit identities.
EXPERIMENTAL for finite diagnostics.
CONJECTURAL / CONDITIONAL for large-sieve or higher-moment targets.
```

## Follow-Up

1. A human should decide whether any Cycle84 raw artifacts deserve a separate
   artifact-preservation branch. They should not be merged into streamlined
   `experimental/` by default.
2. Audit PR #97 against the exact definitions of `eca`, support-wise `emca`,
   and the main Paper D radius conventions before promoting any direct cap.
3. Run PR #97 and PR #98 scripts only after reviewer approval, since this pass
   intentionally avoided executing PR code.
