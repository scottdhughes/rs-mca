# PR triage: 2026-06-27

This triage covers open PRs #122--#129.  The review was done from Git diffs and
PR metadata.  No PR workflow or untrusted PR script was executed during the
triage pass.

## Summary table

| PR | Contributor | Topic | Integration decision |
| --- | --- | --- | --- |
| #122 | AllenGrahamHart | M2 challenge-map pullback ledgers for adjacent high-agreement rows | Integrate into `experimental/data/adjacent-ledgers/` as protocol-ledger accounting.  Not a new MCA numerator row. |
| #123 | AllenGrahamHart / Claude | Reserve272/288/313 board promotion | Integrate conservatively: these are proved as subsumed tangent-floor rows, not independent reserve-scale mechanisms. |
| #124 | AllenGrahamHart / Claude | Cycle116/119 prize gate via tangent floor | Integrate as a status correction: the `>2^-128` gate is unconditional via tangent floor, while the exact `52,747,567,092` numerator remains conditional on finite Cycle84 imports. |
| #125 | Scott Hughes | L1 `d=3` proper-subgroup twisted collision bound | Integrate as an experimental L1 theorem note with imported Katz/Gauss input.  No leaderboard row. |
| #126 | AllenGrahamHart | F1 arbitrary-anchor residue-cloud floors | Integrate as F1 theory infrastructure and counterexample/audit material.  No leaderboard row. |
| #127 | AllenGrahamHart | M1 all-line Hankel aperiodic ledger | Do not merge wholesale.  Keep a distilled audit/roadmap note only; the PR is too large and incremental to promote as a final M1 theorem. |
| #128 | Scott Hughes | Public board L1 result cards | Mostly already represented by existing update rows; use only conservative status wording. |
| #129 | Vadim Avdeev | L1 monomial dyadic descent survivor families | Integrate as a bounded monomial-prefix toy theorem and audit checklist.  No arbitrary-word/list/MCA claim. |

## Leaderboard and public board decisions

The only public leaderboard changes justified by this batch are status
corrections for already-integrated high-agreement tangent material:

1. `Cycle116` and `Cycle119` should keep the printed bad-slope numerator
   `52,747,567,092` as computation-dependent.  However, the weaker
   prize-facing gate `epsilon_mca > 2^-128` is unconditional at agreements
   `262` and `263`, because the moving-root tangent floor gives respectively
   `251` and `250` bad finite slopes and only seven slopes are needed over
   `F_17^32`.
2. `reserve272`, `reserve288`, and `reserve313` should no longer be displayed
   as open targets.  They are proved lower-bound rows, but only in the
   subsumed sense: the generic tangent floor gives `241`, `225`, and `200`
   bad slopes respectively, while strict352 gives an independent quotient-core
   mechanism in the same range.
3. None of #125, #126, #127, #128, or #129 adds a new prize-facing numerator
   or threshold row.  They are theory infrastructure, audits, or bounded toy
   examples.

## Theory-note decisions

The following notes are useful and should stay in `experimental/`:

- `experimental/notes/l1/l1_prefix_dual_d3_subgroup_twisted_collision_bound.md`
  records a proper-subgroup `H^{2k}` collision theorem for the odd moments
  `1,3,5`, conditional only on standard one-variable Katz/Gauss inputs.
- `experimental/notes/l1/l1_monomial_dyadic_descent_survivors.md` records a
  monomial-prefix dyadic descent gate and survivor classification for the
  `F_17^32`, `|H|=512`, `deg P <= 256` toy lane.
- `experimental/notes/f1/f1_arbitrary_anchor_locator_split.md` sharpens the
  arbitrary-anchor F1 picture: free support packing is capped by
  `floor((n-k)/sigma)`, and any larger packet must pass dense-overlap
  residue-direction compatibility gates.
- `experimental/data/adjacent-ledgers/high_agreement_adjacent_ledgers_summary.md`
  gains challenge-map pullback ledgers: largest-fiber envelopes, bounded-degree
  rational maps, and rank-aware linear challenge maps.

For PR #127, only the distilled note
`experimental/notes/m1/m1_all_line_hankel_aperiodic_packet_audit.md` is
promoted.  It records the exact object and useful reductions, but keeps the
status at `AUDIT / PROOF PROGRAM / EXPERIMENTAL`.

## Non-promotion rules applied

- No generated PR claim was promoted from conditional or computational status
  to theorem status without an existing in-repo theorem backing it.
- The Cycle84 exact numerator remains conditional/computation-dependent.
- The M1 all-line aperiodic PR is not treated as the final M1 polynomial bound.
- The L1 monomial and `d=3` notes are not treated as arbitrary-word locator
  local limits.
- F1 arbitrary-anchor examples are not treated as extension-line lift theorems.
