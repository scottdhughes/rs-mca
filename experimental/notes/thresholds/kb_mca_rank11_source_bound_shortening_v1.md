# KoalaBear nested source-bound shortening ladder

Status: proved source-bound adapter; zero active-v4 ledger movement.

The nested pinned coordinates are now genuine common shortening coordinates
for complete scalar agreement domains.  For each `1<=k<=10`, subtract one
fixed interpolation pair on `T_10`, divide by the locator of `T_k`, and delete
the `k` coordinates.  This preserves every selected slope, support-wise
pair noncontainment, and the pinned direction dimension.

The resulting rows are

```text
(n_k,K_k,m_k)=(2097152-k,1048576-k,1116048-k),
```

with unchanged gaps `1048576`, `67472`, and `981104`.  The first four actual
shortened families carry at least

```text
2,843,853,816,476,423   dimension >=8
   93,708,171,878,891   dimension >=7
    3,087,708,134,499   dimension >=6
      101,738,094,101   dimension >=5
```

bad slopes.

The proof must use complete agreement domains.  A `GF(5)` control refutes
arbitrary coordinate replacement inside an exact bad support.

The next attack is a quotient-rank or chronology-owner theorem for this
compatible ladder, not another local shortening.
