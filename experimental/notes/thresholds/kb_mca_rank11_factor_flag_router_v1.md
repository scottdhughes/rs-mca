# KoalaBear rank-eleven factor-flag router

Status: **PROVED STRUCTURAL ROUTER / ZERO LEDGER MOVEMENT**.

## Result

A globally frozen minimizing-pair selection contains a dense center pair with
core deficiency at most four.  At cutoff `tau=1936`, center every low-margin
pair at that pair and form the one- or two-dimensional subcode spanned by the
two endpoint differences.

A two-level annihilator-flat count proves that, unless an actual difference
subcode extends to a three-dimensional direction subcode with at least
`23354` common zeros inside the center core, the complete bad-slope count is
at most

```text
274978667290066176,
```

leaving exact slack `2060821328911` against the KoalaBear budget.

The terminal subcode contains an actual pair-difference row space and has a
squarefree deployed-coordinate common locator of degree at least `23354`.

## Exact profile

```text
dense center subset H0 = 1116044
low pair core floor A  = 1114112
center intersection h  = 133004
Z2                     = 117731
Z3                     = 23354
terminal lines N1      = 8415196932
two-plane slots N2     = 382360905
pair caps Q1,Q2        = 15,255
fixed-pair slope cap   = 983040
```

At `Z3=23355`, exhaustive `Z2` optimization is over budget by
`15117920635889`.  This is the exact adjacent wall of the declared profile.

## Nonclaims

The three-dimensional subcode is not paid, not unique, and not an active-v4
owner.  The packet does not cancel edge factors independently, move a ledger
atom, pay rank eleven, or close KoalaBear.
