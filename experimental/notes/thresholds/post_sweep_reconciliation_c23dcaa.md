# Post-sweep reconciliation: the five hard inputs after the c23dcaa wave

## Status

`AUDIT (reconciliation).  Successor to post_sweep_bracket_reconciliation.md,
in the same bookkeeping pattern: the board moved by ~24 integrated PRs
(#699-#722, integrated at c23dcaa) plus two open packets (#723, #725), and
every one of agents.md's five hard inputs moved.  This note records, per hard
input, what is now proved and what is open after the wave; it gives the exact
supersession/bracket map for the printed image-face numbers; and it credits
every load-bearing external author.  No new mathematics: every claim below is
proved in its cited PR/integrated file, and this note re-derives none of them.
Verifier: experimental/scripts/verify_post_sweep_reconciliation_c23dcaa.py.`

Label key (agents.md dialect): **PROVED** / **CONDITIONAL** / **CONJECTURAL** /
**EXPERIMENTAL** / **AUDIT** / **COUNTEREXAMPLE**.  PR numbers are the
integrated notes' own lane/consumer labels; every note and certificate cited
below exists at its cited path on this tree (checked by the verifier).  No
`.tex`/`.pdf` is touched.  `#723` and `#725` are open at the time of writing
and appear as OPEN weave items.

---

## Per-input state (one line each)

1. **Witness-exhaustive atlas.** `#706` print-audit forces **NO print change**
   (42 anchors / 9 routes current); `#713` `(CAT)` ledger proves exhaustion
   composes but blocks full summation at `{C3,C7,C8,C9}`; `#725` (open) pays
   C3's row-independent coset/ramification reading exactly and proves the
   row-dependent common-factor reading is exactly `binom(n,b)` (non-censusable),
   shrinking the blocked set to `{C7,C8,C9}` on that reading.
2. **Image-scale MI+MA / direct Sidon.** avdeevvadim `#716` banks the
   owner-rooted finite localization (primitive Sidon payment OPEN) and states
   the charge-preserving semantic-or-signed dichotomy; `#717` discharges
   `#716`'s heavy-fiber interface on the locator-prefix chart (hereditary
   reduces to plain emission, CONDITIONAL on atlas (A2) + the extension);
   DannyExperiments `#719` pays structured equal-block signed profiles and hits
   a declared mixed-sign wall; `#718` proves `R=2` growing-characteristic
   image-scale flatness; `#711` walls the two-band habitat denominator; `#723`
   (open) census-locates the first-match mask as the load-bearing clause.  The
   single remaining prefix-chart gap is the signed-or-semantic dichotomy, entry
   point = first-match structure.
3. **Residual ray compiler.** DannyExperiments `#704` transverse-extends the A6
   all-witness line-section compiler (one line / one chart); `#721` gives the
   canonical reduced rational-host exact compiler with a witness-exhaustive
   prefix/remainder incidence bijection for denominator degree `d>1` (PROVED
   under reduced-host hypotheses, no global natural-scale inequality, score
   `0/2`).  Both act on the prefix-to-line face `#717` consumes.
4. **Complete profile-envelope comparison.** The wave adds no new
   profile-envelope note; `profile_envelope_completeness.md` already reduces
   input 4 to inputs 2 + 3, and `#705` hardens the image-face bracket lower end
   `0.160847` to a `k<=5` family ceiling.  **`rho* in [0.160847, 0.405465]`
   survives the wave** (section 6).
5. **Lower reserve / unsafe side.** `#699` pays O5c for the quotient /
   Euclidean-remainder / Chebyshev classes (deep-remainder `w<r` WALL); `#712`
   builds the partial-occupancy atlas and kills the deep-remainder field-drop
   route (Theorem DR); DannyExperiments `#714` refutes `#712`'s instance-level
   no-list verdict (`F_169` guaranteed list `6` > identity floor `1`), so the
   deep-remainder side reopens as payable by label-factoring; `#720` walls the
   Grand List projective-line lift; `#722` gives the rank-15 locator-saturation
   normal form (eliminates no `M`); `#707`/`#710` localize O7 (upper-half
   secant ceiling; combined reserve `max{P,E}`); `#708` roots the order-two
   band failure in a heavy realized boundary fiber.  Residual = O7 non-list
   tangent-floor `E` content + the reopened deep-remainder route.

---

## 1. Hard input 1 — the witness-exhaustive atlas: the reduction chain

The wave leaves the printed atlas statements unchanged and reduces the residual
to a smaller named cell set.  Stated with every hypothesis visible:

1. **(A2) coverage-uniformity is untouched.** `#706`
   (`a6_atlas_print_audit.md`) audits hard input 1 against the A6 chart-payment
   wave and returns **NO PRINT CHANGE FORCED**: 42 atlas-specific tex anchors
   across 9 routes verified current (0 SUPERSEDED, 0 TIGHTENED, 0 CONTRADICTED;
   5 STILL-OPEN routes `U1, ATL, LNU, NEG, NCH`; 2 CURRENT-PROVED `POA, CNS`; 1
   PARTIAL `FIN`; 1 DEFINITIONAL `DEF`).  The A6 wave pays a
   selector-independent `poly(r)` distinct-slope bound for **one** named
   completed-witness chart on **one** received line; it is not a consumer of
   condition **(A2)** / `def:first-match`, whose "uniformly in every received
   line" quantifier is the sharpest honest form and remains open.  *(baseline:
   holmbuar `#536`, `atlas_missing_witness.md` — witness covering is nearly
   free; the hard content of (A2) is a paid exhaustive atlas.)*
2. **Exhaustion composes = THEOREM (PROVED, unconditional).** `#713`
   (`atlas_cat_cell_ledger.md`) turns the scattered per-cell payments into one
   per-cell ledger; the covering half of (A2) composes unconditionally.
3. **Per-paid-cell summation composes on the 5 paid cells (PROVED on the stated
   regimes).**  `#713`.
4. **Full-catalogue summation is BLOCKED at four named cells (CONDITIONAL).**
   `#713`'s `(CAT)` residual is
   ```text
   {C3 planted census, C7 projection degree, C8 higher-dim core, C9 Sidon payment},
   ```
   which `#713` itself reads as collapsing to the manuscript's own hard inputs
   **3** (C8 higher-dim core → residual ray compiler) and **2/4** (C9 → Sidon
   payment) plus **one local combinatorial census** (C3).  `(UNIF)`, the outer
   per-line quantifier, is prerequisite `(CAT)`.
5. **C3 → PARTIAL, splitting on the generator reading (`#725`, open).**  The
   `c3_planted_divisor_census.md` packet supplies and pays the constructible
   family `𝒫_b` for two of `def:algebraically-planted`'s five generator types:
   - **row-independent (multiplicative-coset / automorphism-fixed-point /
     quotient-fiber / ramification):** **PROVED, unconditional, all `N`** — an
     exact `sigma(N)` coset census, ramification subsumption, and a dihedral
     extension;
   - **row-dependent (unrestricted common-factor / received-line-resultant):**
     **PROVED non-censusable** — the unrestricted reading is exactly
     `binom(n,b)` profiles (exponential), so it is not paid this way.

   **Reduction chain, net.** On the row-independent reading of C3, the `(CAT)`
   blocked set shrinks
   ```text
   {C3, C7, C8, C9}   --(#725, row-independent reading)-->   {C7, C8, C9},
   ```
   with C8 → hard input 3 and C9 → hard input 2/4 as in step 4.  The
   row-dependent C3 reading stays open and is shown non-censusable, i.e. it is
   not a coverage gap payable by a subexponential planted census at all
   (`#725` cites `routing_exhaustiveness.md` `#627`, `c7_routing_spectrum.md`
   `#625` for the "not a coverage gap" reading).

**Credit.** A6 wave: **DannyExperiments** (`#659,#671,#676,#687,#697`) and
**holmbuar** (`#681` preflights, `#536` baseline).  `(CAT)` ledger and C3
census: holmbuar / this program, on the manuscript's own printed catalogue.

## 2. Hard input 2 — image-scale MI+MA or direct Sidon: the interlocking chain

This is the face that moved most.  The chain, proved-vs-open:

- **avdeevvadim `#716` — the owner-rooted bank + the dichotomy (PROVED finite
  localization / OPEN primitive Sidon payment).**
  `owner_rooted_dense_band_localization_v1.md` (with
  `owner_rooted_positive_support_localization_v1.md`) banks seven finite
  reductions before the source-specific estimate: exact norming-dual
  representation of the band excess, unique noncommon-slope ownership of
  positive mass, an exact `(a-1)`-packing after strict-excess/repeated-rim
  removal, full-image-scale payment of sparse complete bands, and a
  projected-energy split whose low-energy class is paid by interpolation.  The
  companion audits `primitive_signed_payment_barrier_v1.md` (the
  **charge-preserving semantic-or-signed dichotomy**: a source-algebraic
  heavy-fiber inverse follows from *hereditary* source-algebraic emission, not
  from current emission syntax) and `arbitrary_mask_idempotent_guardrail_v1.md`
  (an abstract `L^{1/2-1/q}` kernel-sign counterexample — max fiber `e^{o(N)}`
  yet `R_A(f) >= exp((1/2-1/q)N-o(N))` — proving harmonic+rim data alone cannot
  derive the signed payment; toy anchor `1.1182491777` at `q=4`, `G=F_3^6`)
  fix what a payment must and must not use.  **The primitive dense-band Sidon
  estimate itself is not proved.**
- **`#717` — heavy-fiber admissibility transfer (CONDITIONAL transfer + PROVED
  core lemmas + route-scoped COUNTEREXAMPLE).**
  `heavy_fiber_admissibility_transfer.md` discharges the first of `#716`'s two
  open interface obligations *on the locator-prefix chart*: every heavy
  `Phi`-syndrome fiber is a depth-`R` locator-prefix fiber, which the
  manuscript's own prefix-to-line compiler realizes as an admissible rooted
  packet — so `#716`'s **"hereditary" hypothesis drops** there — **PROVIDED**
  `R < char(B)` on the power-sum presentation (`R>=char` fails by Newton
  non-invertibility; the elementary chart holds in every characteristic) and
  `R <= m-2`, and CONDITIONAL on atlas (A2) + the `L695-705` extension.
  Sharpening: band **failure** transfers to the whole first-match residual by
  pigeonhole, so the residual gap is purely rooted-packet admissibility of the
  submask, not band-failure transfer.
- **DannyExperiments `#719` — structured signed local-minority payment (PROVED
  profilewise / mixed-sign wall declared).**
  `signed_local_minority_fixed_composition.md` pays exact equal-block-occupancy
  profiles via product-Johnson distance, product-shadow and block-centered
  Plotkin fiber caps (deployed nine-partial profile: fiber upper bound `153`;
  the displayed `441080640` is a sum over a permutation orbit, not an attained
  count).  Its §7 exhibits a balanced mixed-sign profile defeating block
  Plotkin at all four recorded dyadic rescalings, leaving as the exact next
  object "any nonempty primitive first-match residual inside this balanced
  mixed-sign profile" — the same object `#716`'s dichotomy names.
- **`#718` — `R=2` growing-characteristic cycle flatness (PROVED, one classical
  mixed Weil estimate).**  `r2_growing_characteristic_cycle_flatness.md` proves
  image-scale Fourier flatness and the direct max-fiber bound for the order-two
  power-sum map `Phi(S)=(sum t, sum t^2)` at realized image scale
  `A_eff=Q^2`, over growing characteristic — removing the `N/p<=K` restriction
  of the superseded `r2_constant_weil_cycle_flatness.md`.
- **`#711` — two-band habitat denominator wall (route-cut).**
  `exp_ilo_habitat_restriction.md` proves the two-band habitat cannot pay the
  input-2 denominator wall (with `#701` `fenced_energy_pincer.md`: the energy
  is denominator-blind, so there is no pincer).
- **`#723` (open) — signed-minor clause census (EXPERIMENTAL, route-scoped).**
  `signed_minor_payment_clause_census_v1.md` maps, on 42 exact small instances
  (24 sparse + 18 dense, six constraint sets each), which of `#716`'s four
  source-realizability clauses carries the gap between the abstract kernel-sign
  construction and a bounded source-realizable signed packet.  **Headline:** in
  the dense regime (the `#717` §7 superincreasing family, fiber load
  `~(3/2)^B`) exactly one single-clause ablation restores absolute gain growth
  — **clause (iii), the first-match residual mask** (fitted exponent `+0.069`
  vs `-0.07..-0.10` for every other set; `C_iii/B` gain ratio `1.33 -> 2.17 ->
  4.03`); the all-free set does not grow even there (slope `-0.007`), because
  ablating the certified source columns destroys the collision structure.

**Net state of input 2.** On the locator-prefix chart the heavy-fiber interface
is discharged (`#717`), the abstract falsifier is fenced (`#716`), structured
signed profiles are paid (`#719`), the order-two map is flat over growing
characteristic (`#718`), and the denominator-pincer route is dead
(`#701`/`#711`).  **The single remaining gap on that chart is the
charge-preserving signed-or-semantic dichotomy**, and the census evidence of
`#723` says the signed bound lives in the **first-match structure** (clause
(iii)) — that is the evidence-backed entry point for a proof.

## 3. Hard input 3 — the residual ray compiler

- **DannyExperiments `#704`** transverse-all-parameter-extends `#697`'s
  selector-free all-witness line-section compiler
  (`a6_all_witness_line_section_compiler.md`, updated in the wave), keeping the
  same one-line / one-chart scope (its Nonclaims); the atlas coverage gap is
  unchanged (`#706` addendum).
- **`#721` — canonical reduced rational-host exact compiler (PROVED under the
  reduced-host hypotheses).**  `canonical_reduced_rational_host_compiler.md`
  proves that a section-nonpositive received line already admitting one reduced
  rational-host presentation has a unique canonical presentation and a
  witness-exhaustive prefix/remainder compiler; the genuinely new part is the
  **general denominator degree `d>1` normal form and incidence bijection**
  (the separating `d=1` case is already in source).  It does **not** extract a
  presentation from an arbitrary `J<=0` line; its direct scale `M_RH` is not
  `#714`'s image-normalized partial-occupancy scale and is not compared with
  the complete profile envelope; the global deduplicated `LineRay` census
  (pending `#715`) stays open.  Official score `0/2`.

**Interface with the input-2 face.** `#717`'s transfer relies on "the
manuscript's own prefix-to-line compiler" to realize a heavy locator-prefix
fiber as an admissible rooted packet; `#721` is exactly a **witness-exhaustive
prefix/remainder incidence bijection** on the reduced rational-host stratum,
broadening that compiler to `d>1` denominators.  The two meet on the
prefix-to-line face: `#721` supplies the local incidence normal form, `#717`
consumes the realization, and the object neither pays — the global
natural-scale (`LineRay`) inequality — is what `#714`/`#715` scope.

## 4. Hard input 4 — complete profile-envelope comparison

The wave adds **no new profile-envelope note**.  The standing reduction is
`profile_envelope_completeness.md` (hard input 4, base `36de5bf`): it closes
the input-4-unique assembly wrapper and pins the single residual, showing it is
**not an independent open object but exactly inputs 2 and 3**.  Nothing in the
c23dcaa wave reopens that residual.  The only input-4-adjacent movement is the
image-face bracket lower end (section 6): `#705` hardens `0.160847` from "the
`#694` champion" to a **`k<=5` family ceiling**.  *(No note on this tree carries
a `#709` label; the input-4 factoring reused by `#713` is
`profile_envelope_completeness.md`.)*

## 5. Hard input 5 — lower reserve / unsafe side: the current residual statement

- **`#699` — O5c paid for three profile classes (PARTIAL).**
  `lower_reserve_o5c_profile_lists.md` pays the "any larger list" clause of
  `prop:simple-pole-lower` for the **quotient** (PROVED), **Euclidean-remainder
  `w>=r`** (PROVED), and **Chebyshev** (PROVED-via-quotient) classes, routed
  through the collision-aware pole instead of the separating-pole route; the
  **deep-remainder `w<r`** regime is a localized WALL.  Route **O7** stays OPEN
  (per audit `#693`); the §7 coupling lemma proves **no profile-list reaches
  O7**.
- **`#712` — the partial-occupancy atlas, field-drop route DECIDED NEGATIVE
  (Theorem DR).**  `lower_reserve_deep_remainder_atlas.md` builds the missing
  degree-`c`-interlace partial-occupancy atlas and shows the per-cell fiber sum
  cannot be routed through the collision-aware pole with a field drop — in the
  deep regime **no depth-`w` prefix slot is field-drop-clean** (blocker: the
  full-field remainder coefficient `p_{jc}(R)` sitting additively in every
  quotient slot).  **Theorem DR (field-drop route dead) stands.**
- **DannyExperiments `#714` — reopens deep-remainder as payable (COUNTEREXAMPLE
  to the instance-level verdict).**
  `deep_remainder_partial_occupancy_counterexample.md` proves the corrected
  arbitrary-remainder prefix-image theorem (exact count `J_{t,p,r} = binom(b,t)
  binom(N,p) [x^r]((1+x)^c - 1 - x^c)^p`) and exhibits a verified
  reciprocal-locator (label-factoring) pigeonhole beating the identity floor
  inside a strictly deep cell (**guaranteed list `6` vs identity floor `1` over
  `F_169`**).  So field-drop is not the only route: `#712`'s **instance-level
  no-list verdict is REFUTED as printed**, and its "O5c closes as a list
  problem" corollary is **WITHDRAWN**; Theorem DR and the atlas survive.
- **`#720` — Grand List projective-line lift wall (PROVED route cut).**
  `projective_line_lift_feasibility_wall.md` proves feasibility of every
  integer scalar nested-MacWilliams profile through `T+1`, the exact `K+2`
  shadow endpoint cut (still far above `T`), survival of a `2^61` support model
  under all pairwise/`K+2`-local line laws, and the equivalence of literal GRS
  realization to **one global binary projective-line lift** — which stays OPEN.
- **`#722` — rank-15 locator-saturation normal form (PROVED structural,
  eliminates no `M`).**  `rank15_locator_saturation_normal_form.md` forces every
  putative survivor with `212 <= M <= 218` into one gcd-reduced pencil with
  15-rich pairwise-coprime fibers nearly factoring the domain locator; it is
  not a two-flat ceiling and removes no value of `M` (it consumes the `#703`
  affine-section source).
- **`#707`/`#710` — O7 localized (PROVED route cut + PROVED combined reserve).**
  `identity_crossing_upper_half_route_cut.md` cuts the O7 intermediate crossing
  **strictly above the half boundary** `(n+k)/2`: since the identity coefficient
  field contains the `n`-point domain, `g <= 1/beta <= 1/log_2 n`, so a
  fixed-rate crossing is `O(n/log n)=o(n)` steps from `k+1` while the half
  boundary is a linear distance away.  `two_regime_lower_reserve_frontiers_packet.md`
  proves the combined two-regime pole-tangent lower reserve `max{P,E}`
  (superseding the proposed-manuscript-change section of
  `simple_pole_realizability.md` / L-3 of `#524`, leaving those notes
  unchanged), building on scottdhughes's exact prefix/pole packets, holmbuar's
  realizability analysis, and LegaSage's `#524` audit anchors.
- **`#708` — order-two band failure rooted (PROVED finite reduction / route
  cut).**  `rooted_order_two_band_reduction.md` shows an order-two dyadic band
  failure (avdeevvadim `#677`'s MBR/BCI targets) needs no arbitrary top-order
  dual witness: orthogonal projection roots every failure in a heavy realized
  boundary fiber with actual same-boundary residual support pairs.  The
  semantic pair-to-owner map is what remains.

**Current input-5 residual statement.**  The list route is closed *as a
field-drop problem* (Theorem DR) but **not** as a list problem (`#714`):
deep-remainder reopens as payable by label-factoring.  The unsafe side is
therefore **O7 non-list tangent-floor `E` content** (`#699` coupling lemma +
`#707` upper-half cut + `#710` combined reserve) **plus the reopened
label-factoring deep-remainder route** (`#714`), with the Grand List
inequality (`#720`) and the rank-`M` count (`#722`) still open above.

---

## 6. Supersession and bracket table

The unconditional image-face bracket is unchanged by the wave:

```text
rho*  in  [0.160847, 0.405465]        (SURVIVES the c23dcaa wave)
```

- **Lower end `0.160847`** — the `b=24` (`k=4`) stacked-trade champion
  (`f_inf=190`, `L1_inf=4192627`), `rho = (ln f + ln L)/b - ln 2`.  `#705`
  (`comb_trade_champion_k5.md`) hardens it from "champion" to a **`k<=5` family
  ceiling**: `k=5` flat gives `f_inf=2072, L1_inf=57376057, rho_inf=0.156900 <
  0.160847` (two independent algorithms + mass conservation `= 2^30`), and the
  non-uniform-weight search returns the `b=24` champion itself as the unique
  maximum (AP weights, up to affine; `k=5` weight max `0.160018` at weights
  `(0,1,2,7,8)`).
- **Upper end `0.405465 = ln(3/2)`** — the canonical-transversal VC compression
  chain (`ilo_moment_closed_consumer.md`, `image_face_print_audit.md`).
  **Unchanged.**

| printed statement (integrated file) | status after the c23dcaa wave |
|---|---|
| `rho* in [0.160847, 0.405465]` (predecessor `post_sweep_bracket_reconciliation.md`) | **survives**; lower end now a `k<=5` family ceiling (`#705`) |
| `0.160847` = `#694` champion (`comb_trade_champion.md`) | current; hardened to family ceiling by `#705`, **not superseded** |
| `#712` instance-level "no deep-remainder list / O5c closes as a list problem" (`lower_reserve_deep_remainder_atlas.md`) | **REFUTED / WITHDRAWN** by DannyExperiments `#714`; Theorem DR (field-drop dead) survives |
| L-3 lower reserve / proposed-change section of `simple_pole_realizability.md` (`#524`) | **superseded** by `#710`'s combined `max{P,E}` (superseding note; integrated notes left unchanged) |
| `r2_constant_weil_cycle_flatness.md` (bounded `N/p<=K`) | **superseded** by `#718`'s growing-characteristic flatness (unbounded `N/p`) |
| `#691` T1 as printed (carried by predecessor's map) | already `#700` T1'; unchanged by this wave |

**No printed bracket number is superseded by the c23dcaa wave.**  The
supersessions above are of *route verdicts and conditional lemmas*, not of the
image-face bracket endpoints.

---

## 7. Audit-before-consume

The three integrated results this synthesis leans on hardest, plus the two
open packets it weaves, were re-verified from `c23dcaa` (from their own
branches for the two open ones) while writing this note:

```text
#713  verify_atlas_cat_ledger.py --check                     RESULT: PASS (219/219)
#717  verify_heavy_fiber_admissibility_transfer.py --check   RESULT: PASS (77/77)
#699  verify_lower_reserve_o5c.py --check                    RESULT: PASS (49/49)
#725  verify_c3_planted_divisor_census.py       (open, ours) RESULT: PASS (82/82)
#723  verify_signed_minor_payment_clause_census_v1.py (open) RESULT = PASS (470 checks)
```

`#705`'s `verify_comb_trade_champion_k5.py --check` is a ~6-12 min `k=5` census
recompute; it was **not** rerun here, and its `k=5` numbers are consumed
from the integrated packet.  Its two load-bearing constants (the champion
`rho=0.160847` and the `k=5` ceiling `rho=0.156900`) are **independently
recomputed** from `(f,L,b)` by this note's own verifier via
`rho=(ln f + ln L)/b - ln 2`, together with `ln(3/2)=0.405465`.

## Nonclaims

No new mathematics.  This note re-derives no cited result; each row of
sections 1-6 is proved in its cited PR or integrated file.  It does not decide
the input-2 signed-or-semantic dichotomy, the deep-remainder label-factoring
payment (`#714`), the Grand List projective lift (`#720`), the rank-`M` count
(`#722`), or the global `LineRay` census (`#715`).  It does not claim the
bracket lower end is final outside the `#705` searched `k<=5` window, and it
does not touch `experimental/asymptotic_rs_mca_frontiers.tex`.  `#723` and
`#725` are open; their headline results are EXPERIMENTAL and PARTIAL
respectively and are recorded as such.

## Files

- Note: `experimental/notes/thresholds/post_sweep_reconciliation_c23dcaa.md` (this).
- Verifier: `experimental/scripts/verify_post_sweep_reconciliation_c23dcaa.py`
  (stdlib-only, deterministic; recomputes the two `rho` constants and
  `ln(3/2)`, asserts every ordering in section 6, checks every cited note /
  verifier / certificate exists at its cited path on the tree, asserts the
  section-1/2/5 residual-set contents, and asserts the audit-before-consume
  pass counts; `--check` and `--tamper-selftest`; runs in `<60s`).
