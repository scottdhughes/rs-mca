# PR batch attribution for 2026-07-03

Status: `ATTRIBUTION / AUDIT`.

Integration commit:

```text
ffc284f8 Integrate DAG and threshold PR packets
```

The integration commit was made by `przchojecki` so that the repository history
stays on `main`, but the mathematical and experimental payload came from the
PR authors below.  This note is the per-PR attribution ledger for that batch.

## Majority contribution: AllenGrahamHart

Allen authored the large majority of the integrated PRs:

```text
#178  DAG maintenance: critical-set invariant, strategy classifier,
      enumeration routes, red-team queue
#179  M5 A384 rank-drop split-locator dedup packet
#180  Row-C E1 value-set sampling pilot
#181  FM1 exact aperiodic first-moment packet
#182  Conjecture F reduction lemmas
#183  Conjecture F dimension-two evidence
#184  XR exchange-energy calculus
#185  spread / sparse syzygy / finite-geometry evidence
#186  L1 petal auxiliary wide-regime evidence
#187  GAP-1 non-equivariant periodic evidence
#188  E5 multiscale resonance evidence
#189  M1 GAP-2 quotient seam arithmetic
#190  F1 extension-import arithmetic
#191  M1 displacement and exchange spectral foundations
#192  M5 stratification partition accounting
#193  adjacency staircase localization
#194  paid ledger function compiler
#195  quotient census and dodge-selection compilers
#196  planted arithmetic list compiler
#197  L1 petal and PMA evidence packets
#199  M1 SPI and deficiency-one chart lemmas
#200  Conjecture F consumer-scope audit
#201  Conjecture F many-sparse census
#202  stripped XR inverse candidate scan
#203  generator-economy design search
#206  quotient value-set certification lemmas
#207  Lean tier-one certification map
#208  E23 A=425 unsafe-side certificate
```

Allen's batch contribution should be cited for the prize DAG, most of the new
threshold compiler infrastructure, the Conjecture F evidence layer, the M1/L1/F1
proof-program packets, the Lean certification-map update, and the A425 unsafe
fallback side of the new prime-field adjacent threshold row.

## Vadim Avdeev

Vadim authored:

```text
#204  M2: A=426 two-core exact finite-slope threshold row
```

This is the safe-side half of the new prime-field adjacent finite-slope row.
Together with Allen's A425 unsafe fallback in #208, it gives the displayed
A425/A426 exact gate for

```text
C = RS[F_p,H,256], |H|=512, p=22275*2^120+1.
```

## DannyExperiments / Gia

DannyExperiments opened the CAP25 sparse-sigma audit PR, with local commit
authorship recorded as Gia:

```text
#198  CAP25 sparse sigma first-layer audit
```

This packet is integrated as an audit/proof-program note, not as a new
leaderboard theorem.

## Integration notes

- Stale README, site, and `towards-prize` edits from older branch bases were
  intentionally not taken.
- `experimental/agents-log.md` records this as a mixed-status integration:
  proved local finite-slope row, audits, experimental evidence, and roadmap
  infrastructure.
- The public site promotes only the A425/A426 adjacent finite-slope threshold
  row as a new leaderboard item; the rest remains experimental support material
  until independently audited or promoted.
