# M1 KoalaBear full-outside carrier-incidence splice certificate

This directory binds the exact zero-ledger successor to the degree-195
effective-multiplier owner.  It combines the source-slack floor, rich-line
moving-zero bound, canonical basis atlas, full-outside carrier shrink, and
rank-nine one-cut compiler.

Replay from the repository root:

    python3 experimental/scripts/verify_m1_kb_rank9_full_outside_carrier_incidence_splice_v1.py --check
    python3 -O experimental/scripts/verify_m1_kb_rank9_full_outside_carrier_incidence_splice_v1.py --check
    python3 experimental/scripts/verify_m1_kb_rank9_full_outside_carrier_incidence_splice_v1.py --tamper-selftest
    python3 -O experimental/scripts/verify_m1_kb_rank9_full_outside_carrier_incidence_splice_v1.py --tamper-selftest
    /usr/local/bin/sage experimental/scripts/verify_m1_kb_rank9_full_outside_carrier_incidence_splice_v1.sage

Replay the load-bearing predecessor independently:

    python3 experimental/scripts/verify_m1_kb_rank9_bounded_slack_effective_multiplier_frobenius_owner_v1.py --check
    python3 experimental/scripts/verify_m1_kb_rank9_bounded_slack_effective_multiplier_frobenius_owner_v1.py --tamper-selftest

To inspect the canonical expected payload after all bound sources are frozen:

    python3 experimental/scripts/verify_m1_kb_rank9_full_outside_carrier_incidence_splice_v1.py --print-certificate

The Python verifier scans every integer slack from 196 through 913,631.  It
proves exact payment on 196--67,466 and 236,098--913,631, with the coarse M2b
carrier exit beginning at 330,336.  It also verifies an exact abstract scalar
packing at every integer in the remaining interval 67,467--236,097.  That
packing is a compiler route cut, not a Reed--Solomon selector or deployed
counterexample.

Sage independently checks all load-bearing transition endpoints and supplies
sharp positive- and negative-`x` moving-zero pencils over `GF(101)`.  Those
fixtures are exact toy controls, not a deployed census or proof substitute.

The predecessor owner, first-match order, and ledger are unchanged.  The
intermediate low-`x` determinant/source packing problem, non-full-outside
source load, `U_Q`, residual `U_A`, rank nine, and KoalaBear remain open.
