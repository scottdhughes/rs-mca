# KoalaBear source-bound shortening certificate

This certificate freezes the ten shortened row triples, inherited loads and
dimension floors, invariant gaps, the support-replacement counterexample, and
the exact nonclaims.

Run the primary, independent, and manifest verifiers in
`experimental/scripts/`.
