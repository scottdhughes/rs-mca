# M31 rank-seven weighted-head and interlaced-source route cut

This packet proves source-bound weighted-head, full-projective-line,
planted-root, and per-label dual-domain theorems.  The two weighted
identities are

```text
sum_(delta_i<=Q) (g-delta_i) <= R*B_6(Q)
sum_(delta_i<=Q) q_i         <= g*Phi_Q(g).
```

The first rejects the predecessor's `H_Q`-saturated endpoint histogram.
The exact full-line recurrence then advances the safe rank-seven head
through `Q=26,143`.

At the next one-level specialization `q=delta=26,144`, the inequality-only
top-six relaxation has head `15,776,148` and is too large.  The source
identity adds a planted-root affine cap to every eligible projective-line
slice.  An exhaustive scan over every legal largest class gives

```text
source-lifted head          15,345,533
target margin                  430,399
objective numerator      5,046,040,936,511
largest class                    283,663
residual dimension                 3,862
```

The packet also proves that initial common zeros cannot worsen this scan.
Thus the entire declared one-level marginal is source-impossible.

The dual-domain compiler handles the mixed distributions.  It splits each
normalized-label subclass between the `E0` and planted-root domains, while
the cofactor pivot pays common planted agreements.  At `Q=26,193`, the
declared linear schedule

```text
x(k) = 44,835 - floor(67*k/10)
```

refines all 239 coarse-unpaid largest-line classes.  After rebuilding the
size-prefix maxima, the exact head is

```text
head                   15,775,776
target margin                 156
objective numerator  5,186,744,182,280
remaining survivors             0
```

At `Q=26,194`, the separately named tangent-shift-11 diagnostic has head
`15,800,402`.  Its maximizer has residual dimension `k=3,145` and old cap
`15,762,647`.  Seven exact monotone intervals cover every legal split; the
smallest dual lower bound is `15,762,839`, still 192 above the old cap.
Thus no per-label two-domain split improves this class.  This is a sharp
method route cut, not a source realization or global rank-seven closure.

The proper fixed-`G` endpoint cap is `F_26144=412,817`.  The corresponding
exact cyclic cofactor/lcm relaxation has 39 slices, with planted-root loads
45,300 at load two and 309,672 at load three.  It remains a combinatorial
relaxation, not a common source family.

Finally, the packet proves an exact deployed-parameter source construction:

- one zero boundary anchor and seven distinct companions;
- exact agreement `1,116,023` and exact error weight `981,129`;
- zero-anchored linear rank seven;
- endpoint master degree `g=354,972`;
- seven mixed degree-`67,448` denominators with exact lcm `P`;
- `q=delta=287,524=d-1`, `s=0`;
- exact full gcds `gcd(P*L0,Y-f_i)=Q_i*H_i`;
- no complete ambient `T32` or `T2048` fiber in any certified agreement
  or error support, with the locators also avoiding complete `T2` fibers.

The construction uses the proved pairwise CRT equivalence to produce one
common unit `V`.  Base-field embedding places it inside the deployed
quartic-field Reed--Solomon code.

## Replay

From the repository root:

```bash
python3 experimental/scripts/verify_m31_rank7_weighted_head_interlaced_source_route_cut_v1.py --check
python3 experimental/scripts/verify_m31_rank7_weighted_head_interlaced_source_route_cut_v1.py --tamper-selftest
HOME=/tmp/rs-mca-sage-home /usr/local/bin/sage experimental/scripts/verify_m31_rank7_weighted_head_interlaced_source_route_cut_v1.sage
python3 experimental/scripts/verify_m31_rank7_weighted_head_interlaced_source_route_cut_packet_v1.py
python3 experimental/scripts/verify_m31_rank7_weighted_head_interlaced_source_route_cut_packet_v1.py --tamper-selftest
```

The packet verifier replays normal and optimized Python, independent Sage
arithmetic and finite-field controls, all source hashes, canonical JSON,
schema constants, semantic mutations, hostile JSON, and predecessor packets.

## Explicit nonclaims

- Eight certified codewords are not a forbidden-size list.
- The construction is not a counterexample to the M31 LIST row.
- The packet does not determine the complete list of its received word.
- It gives no upper payment for any v4 atom or signed `Xi_46` refund.
- It does not close the `Q=26,194` primitive class or any higher rank.
- The 39-slice cofactor marginal is not promoted to a source family.
- No block-metric or fixed-template count is converted into
  chronology-valid codeword add-back.
