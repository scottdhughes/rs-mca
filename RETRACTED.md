# Retracted after hostile universal-core audit

Do not cite this branch as a valid rank-twelve three-slope route cut.

The weighted-line cap used at ambient residual dimension `k=K-1` silently
assumed that the rank-one direction had no additional universal coordinates.
If the proper-drop direction vanishes on `u>0` further common-core
coordinates, source-bound cancellation reduces the effective dimension to

```text
k_eff = K-1-u,
```

and the valid rank-one cap is the cap at `k_eff`, which can be larger.  Thus
the claimed comparison at `K=262,712` is not uniform over the actual
source-bound branch.

The immutable pre-retraction head is archived at

```text
archive/kb-mca-rank12-three-slope-invalid-20260817
```

The correct successor must retain `u` and convert the rank-one capacity
inequality into an exact lower bound for the common-locator degree.  Affine
error rank twelve remains open.
